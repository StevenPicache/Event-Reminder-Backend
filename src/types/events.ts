export type Events = {
    id: number
    name: string
    eventType: string
    eventDate: Date
}


export type AddEvent = {
    firstName: string,
    lastName: string,
    eventType: string,
    eventDate: Date,
}
